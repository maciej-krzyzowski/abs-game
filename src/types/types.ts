export type FieldsType = {
  [key: number]: number;
};

export type RollType = {
  result: number;
  count: number;
  totalResult: number;
};

export type OptionsFieldsType = {
  isOpen: boolean;
  selectField: string;
  selectAction: number;
};
